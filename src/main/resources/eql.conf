eql {
  standalone: true,
  cluster.name: "elasticsearch",
  host: "localhost",
  port: 9300,
  local: {
    http.enabled: true,
    action.destructive_requires_name: false,
    path.home: "/Users/xiachen/IdeaProjects/coolmarks/local/",
    transport.type: "netty4",
    bootstrap.memory_lock: true
  }
}
